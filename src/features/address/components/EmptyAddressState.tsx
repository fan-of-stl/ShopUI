import {
  Typography,
} from "@mui/material";

const EmptyAddressState =
  () => {

    return (
      <Typography>
        No addresses found
      </Typography>
    );
};

export default
EmptyAddressState;